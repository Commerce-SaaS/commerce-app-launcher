version: "3.9"

services:
  payments-ms:
    build: ../payments-ms
    ports:
      - "4006:4006"
    env_file:
      - ../.env
    volumes:
      - ../payments-ms:/usr/src/app
      - /usr/src/app/node_modules
    command: npm run start:dev