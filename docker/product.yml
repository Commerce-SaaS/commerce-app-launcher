version: "3.9"

services:
  products-ms:
    build: ../product-ms
    ports:
      - "4001:4001"
    env_file:
      - ../.env
    volumes:
      - ../product-ms:/usr/src/app
      - /usr/src/app/node_modules
    command: npm run start:dev